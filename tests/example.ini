[sqlite]
file = "example.db"

[table.users]
id = integer primary key autoincrement
username = text
lastname = text
firstname = text
groups = text

[table.groups]
id = integer primary key
groupname = text

; [table.relations]
; id = primary key
; fromtable = text
; fromid = int
; totable = text
; toid = int
